# Synthetic Fixtures

Only original, clearly fictional issuer documents and adversarial cases belong here. Fixtures must not reproduce real issuer tables or long passages.

`northstar_revenue.txt` is an original, fictional, machine-readable filing fragment bound by source id and SHA-256 to the exact M1 synthetic validation profile. It is not modeled on or copied from a real issuer report, and it is not evidence of generalized filing support.

`aurora_revenue_growth_m2.txt` is a second original, fictional fragment bound
to the M2 registered-semantics and bounded-retry profile. Its aliases and
adversarial lines exercise fiscal-period, metric, basis, currency, unit, scale,
sign, cutoff, locator, and lineage decisions. Together with the full offline
suite and review evidence it supports bounded scripted M2 Gate completion; it
does not demonstrate source authenticity or generalization to issuer filings.
